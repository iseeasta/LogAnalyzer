# LogAnalyzer
CLI-based multithreaded log analyzer in Java — parses 6000+ log entries using ExecutorService, stores in PostgreSQL via JDBC batch insert, and provides interactive search and analysis through a terminal menu.  
